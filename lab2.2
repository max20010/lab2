#include "pch.h"
#include <iostream>
using namespace std;

int main()
{
	setlocale(LC_ALL, "ru");
	int a, b, c, x1, x2;
	cout << "Введите A " << endl;
	cin >> a;
	cout << "Введите C " << endl;
	cin >> c;
	cout << "Введите B " << endl;
	cin >> b;
	if (c > a && c < b) {
		x1 = c - a;
		x2 = b - c;
	}
	cout << x1 * x2 << endl;	
}
